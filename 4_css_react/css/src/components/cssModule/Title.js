import styles from './Title.module.css';

const Title = () => {
    return (
        <div>
            <h1 className={styles.css_module}>Usando CSS Module</h1>
        </div>
    )
}

export default Title